<%= yield %>

-- 
<% rz = resource.present? && resource.class == User ? resource : @user %>
<% if rz.present? %>
Unsubscribe from all Otherbox emails by visiting <%= unsubscribe_user_url(rz, :secret => rz.delivery_secret) %>
<% end %>
